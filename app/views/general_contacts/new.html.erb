<% if @section %>
	<%= render partial: 'semesters/semester', locals: {semester: @section.semester} %>
	<%= render partial: 'sections/section', locals: {section: @section} %>
<% end %>

<%= render 'form' %>

<% if @section %>
	<%= render partial: 'instructors/instructor', locals: {instructor: @section.instructor} %>
<% end %>
