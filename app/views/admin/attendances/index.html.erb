<h1>Listing attendances</h1>

<table>
  <tr>
    <th>Attendance status</th>
    <th>Meeting</th>
    <th>Enrollment</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

xxx@xxx.xxx do |attendance| %>
  <tr>
    <td><%= attendance.attendance_status %></td>
    <td><%= attendance.meeting %></td>
    <td><%= attendance.enrollment %></td>
    <td><%= link_to 'Show', [:admin,attendance] %></td>
    <td><%= link_to 'Edit', edit_admin_attendance_path(attendance) %></td>
    <td><%= link_to 'Destroy', [:admin,attendance], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Attendance', new_admin_attendance_path %>
