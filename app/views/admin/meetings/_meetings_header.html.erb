<div class="block <%= double_width_if_main(defined? main) %>">
	<p><%= pluralize section.meetings_complete.size, "Meeting" %> complete, <%= pluralize section.meetings_not_complete.size, "Meetings" %> to go</p>
	<h3><%= link_to 'Meetings', admin_semester_section_meetings_path(section.semester,section) %></h3>
	
	<ul class="actions">
		<li><%= link_to 'New Meeting', xxx@xxx.xxx %></li>
	</ul>
</div>
