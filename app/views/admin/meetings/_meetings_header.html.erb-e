<div class="block <%= double_width_if_main(defined? main) %> meetings header">
	<p><%= pluralize section.meetings_complete.size, "Meeting" %> complete, <%= pluralize section.meetings_not_complete.size, "Meetings" %> to go</p>
	<h5><%= link_to 'Meetings', admin_semester_section_meetings_path(section.semester,section) %></h5>
</div>
