
<%= render partial: 'admin/courses/course', locals: {course: @course} %>
<%= render 'objectives_header' %>

<div class="block objectives">
	<header>
		<p>for <%= @course.title %></p>
		<h4>New Objective</h4>
	</header>
</div>

<%= render 'form' %>
