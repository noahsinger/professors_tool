$("#steps").empty( );

<% @how_to.steps.each do |step| %>
	$("#steps").append("<%= escape_javascript(render(partial: step)) %>")
<% end %>

$("#steps").reorderTable( "<%= sort_admin_how_to_steps_url( @how_to ) %>", "<%= form_authenticity_token %>" );
