<div class="block">
  <p><%= general_contact.return_email %></p>
  <h3><%= link_to general_contact.subject, [:admin,general_contact] %></h3>
	<% if general_contact.attachment %>
		<p><%= link_to general_contact.attachment_file_name, general_contact.attachment.url %></p>
	<% end %>
	<ul>
    <li><%= link_to 'Destroy', [:admin,general_contact], confirm: 'Are you sure?', method: :delete, remote: true %></li>
	</ul>
</div>
