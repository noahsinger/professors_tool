<div class="block form grade_requests">
	<%= xxx@xxx.xxx @section, @grade_request], remote: true) do |f| %>
		<%= xxx@xxx.xxx %>
		<ol>
			<li>
				<div class="details">
					<%= f.label :email %>
					<p>Provide your official email address so your grades can be found.</p>
				</div>
				<div class="field"><%= f.email_field :email, {placeholder: xxx@xxx.xxx %></div>
			</li>
			<li class="action">
				<div><%= f.submit "Send Request" %></div>
			</li>
		</ol>
	<% end %>
</div>
