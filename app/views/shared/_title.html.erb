<%= APP_CONFIG['host'].html_safe.gsub(/\<.*\>/,'') %>  &bull;
<% if @section and @section.course %>
	<%= @section.course.short_name %>
<% else %>
	<%= controller.controller_name.titleize %> 
<% end %>

<% if controller.action_name != "index" and controller.action_name != "show" and controller.action_name != "new" %>
	<%= controller.action_name.titleize %>
<% end %>
