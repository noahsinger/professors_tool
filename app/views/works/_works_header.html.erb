<div class="block works header <%= double_width_if_main(defined? main) %>">
	<header>
		<%= link_to new_semester_section_assignment_work_path(@semester, @section, @assignment ) do %>
			<p>Submit Your Solution</p>
			<h5><%= @assignment.title %></h5>
		<% end %>
	</header>
</div>
