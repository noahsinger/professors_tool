<% if material.file_file_name %>
	<li>
		<h5><%= link_to material.title, material.file.url, {:title => material.description} %></h5>
		<%= material.description %>
	</li>
<% else %>
	<a title="<%= material.description %>"><%= material.title %></a>
<% end %>
