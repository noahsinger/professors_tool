<div class="block materials header <%= double_width_if_main(defined? main) %>">
	<header>
		<p><%= pluralize course.materials.count, "File" %> available</p>
		<% xxx@xxx.xxx %>
			<h3><%= link_to "Course Materials", course_materials_path(course, xxx@xxx.xxx %></h3>
		<% else %>
			<h3><%= link_to "Course Materials", course_materials_path(course) %></h3>
		<% end %>
	</header>
</div>
