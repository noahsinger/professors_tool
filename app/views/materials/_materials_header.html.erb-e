<div class="block <%= double_width_if_main(defined? main) %>">
	<header>
		<p><%= pluralize course.materials.count, "File" %> available</p>
		<% xxx@xxx.xxx %>
			<h2><%= link_to "Course Materials", course_materials_path(course, xxx@xxx.xxx %></h2>
		<% else %>
			<h2><%= link_to "Course Materials", course_materials_path(course) %></h2>
		<% end %>
	</header>
</div>
